name: material_io_ext
description: material.io extenstion methods. collection of extenstions methods to create widgets as per material.io guidelines.
version: 0.0.2
homepage: https://github.com/ravikovind/material_io_ext

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter: